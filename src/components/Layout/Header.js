import { Fragment } from 'react';

const Header = (props) => {

    return <Fragment>
            <header></header>
            <h1>Meals</h1>
            <button></button>
            <div></div>
        </Fragment> 
};

export default Header;